# SDL2 bindings for Mojo 
This is an expanded version of [mojo-sdl](https://github.com/msteele/mojo-sdl). Adapted to work with Mojo 0.26.1.

Ensure SDL2 is installed:
```bash
apt install libsdl2-dev
```

Run example with
```bash
mojo example.mojo
```